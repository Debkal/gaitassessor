import numpy as np
import mediapipe as mp
import os
import csv
from enumvars import Directory as dir

class Pose_extractor.py:
    def __init__(self, scheme):
        self.scheme = scheme

    def getPose(video_path,video_length = 64):
        posedata = "example.csv"
        return posedata

